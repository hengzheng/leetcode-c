int reverse(int x) {
    double ret = 0;
    int max = 1<<31;
    while(x != 0){
        int rem = x % 10;
        x = x / 10;
        ret = ret * 10 + rem;
        if (ret < -max || ret > max - 1){
            return 0;
        }
    }
    return ret;
}
